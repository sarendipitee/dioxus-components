All 12 placements (4 sides × 3 alignments) mapped around a central trigger. Click any button to move the popover to that position — it closes and reopens anchored at the new placement so floating-ui computes fresh coordinates. The popover label always shows the requested placement; if your viewport clips that side, floating-ui flips it automatically and `data-side` / `data-align` reflect the resolved position.
